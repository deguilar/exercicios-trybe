// Também, é possível declarar funções e atribuí-las a variáveis, como no exemplo a seguir, em que a função printName é atribuída a uma variável.

const printName = function () {
  const myName = 'Lucas';
  return myName;
};

console.log(printName());